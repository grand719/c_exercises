#include <stdio.h>

#define MAXLINE 1000

int getWordsCount(char words[][], int max);

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}

int getWordsCount(char words[][], int max)
{
    char line[];
    int c, i, j;
    for (i = 0; i < max - 1 && (c = getchar()) != EOF && c != '\n'; i++)
    {
    }
}
